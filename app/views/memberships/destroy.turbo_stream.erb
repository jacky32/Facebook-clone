<% if notice %>
  <%= turbo_stream.replace "member-requests", partial: "communities/member_requests", locals: { community: @community } %>
  <%= turbo_stream.replace "member-list", partial: "communities/member_list", locals: { community: @community } %>

  <%= render_turbo_stream_flash_messages %>
<% elsif alert %>
  <%= render_turbo_stream_flash_messages %>
<% end %>